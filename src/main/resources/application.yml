spring:
  application:
    name: products

  datasource:
    url: jdbc:postgresql://localhost:6969/products_db
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
